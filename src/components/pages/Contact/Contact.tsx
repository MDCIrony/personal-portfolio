import React from "react";
import StyledPage from "../../../constants/StyledComponents";

export default function Contact(): React.ReactElement {
  return (
    <StyledPage>
      <h1 className="text">Contact {`<en construcción...>`}</h1>
    </StyledPage>
  );
}
