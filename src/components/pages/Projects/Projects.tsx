import React from "react";
import StyledPage from "../../../constants/StyledComponents";

export default function Projects(): React.ReactElement {
  return (
    <StyledPage>
      <h1 className="text">Projects {`<en construcción...>`}</h1>
    </StyledPage>
  );
}
